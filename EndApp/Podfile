# Uncomment the next line to define a global platform for your project
 #platform :ios, '9.0'

target 'EndApp' do
  
  use_frameworks!
  # Pods for EndApp
  #platform :ios, '8.0'
  pod 'SDWebImage', '~> 5.0'

  target 'EndAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EndAppUITests' do
    # Pods for testing
  end

end
